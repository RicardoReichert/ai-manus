# Project Rules & User Preferences

- **Environment:** Windows WSL (Windows Subsystem for Linux).
- **Local URLs:** Always use `0.0.0.0` (e.g. `http://0.0.0.0:5173`) when providing browser links to the user.
